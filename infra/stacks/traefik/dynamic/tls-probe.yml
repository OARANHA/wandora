http:
  routers:
    tls-probe:
      rule: "Host(`tls-probe.wandora.com.br`) && PathPrefix(`/api`)"
      entryPoints:
        - websecure
      service: api@internal
      tls:
        certResolver: letsencrypt
