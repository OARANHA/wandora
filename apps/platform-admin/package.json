{
  "name": "@wandora/platform-admin",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.13.0"
  },
  "scripts": {
    "check": "node --check src/*.mjs test/*.test.mjs",
    "test": "node --test --test-concurrency=1 test/*.test.mjs",
    "verify": "npm run check && npm test",
    "start": "node src/main.mjs"
  }
}
